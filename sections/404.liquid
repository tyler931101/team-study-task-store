<section style="padding:5rem 1rem;text-align:center">
  <h1 style="font-size:3rem;margin-bottom:1rem">Page not found</h1>
  <p style="margin-bottom:2rem">Sorry, the page you were looking for doesn’t exist.</p>

  <a
    href="/"
    style="padding:.75rem 1.5rem;background:{{ section.settings.button_color }};color:white;text-decoration:none;border-radius:.25rem"
  >
    {{ section.settings.button_label }}
  </a>
</section>

{% schema %}
{
  "name": "404 page",
  "settings": [
    {
      "type": "text",
      "id": "button_label",
      "label": "Button text",
      "default": "Back to home"
    },
    {
      "type": "color",
      "id": "button_color",
      "label": "Button color",
      "default": "#333333"
    }
  ],
  "presets": [{ "name": "404 page" }]
}
{% endschema %}
